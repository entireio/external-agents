You are a FRESH session responding to the Noon Curveball for Track 3. First reconstruct prior context by running 'entire release-gate handoff --repo release-gate' and reading release-gate/docs/architecture/02-pre-noon-stable.md. Then run 'entire graph impact --symbol build_bundle --repo .' to confirm affected paths. Then create release-gate/docs/architecture/03-curveball-response.md documenting: (1) INVALIDATED ASSUMPTION - the integrated workflow was assumed to emit evidence/lifecycle events in a single fixed format; the curveball introduced a NEW transcript/lifecycle-event format while existing users still emit the original. (2) GRAPH IMPACT (run before editing) - build_bundle is the single assembly boundary; collect_checkpoint_signals feeds both scoring and handoff; all downstream (to_silver->features->scoring->writeback) consumes the normalized bundle, so the safe fix is to adapt at the parse boundary and keep the bundle contract stable. (3) DESIGN CHANGE - added release_gate/events.py: ONE version-detecting parser normalizes BOTH v1 (type-based) and v2 (event/payload-based) lifecycle events into the same internal model, then events_to_bundle assembles the existing evidence-bundle contract. No duplication. Unknown events are counted not fatal; corrupt/truncated lines tolerated; incomplete input yields a PARTIAL bundle (ingest.partial=true) surfaced as a PARTIAL banner in the PR comment so incomplete context is never presented as authoritative. New 'entire release-gate ingest --events <jsonl>' subcommand. (4) TESTS - tests/test_events.py covers original format, new format, unknown events (no crash), and incomplete input; existing 11 tests still pass (16 total). (5) WHY SAFE - existing behaviour preserved (collect via entire CLI unchanged); downstream untouched; partial results clearly flagged. Keep it concise and accurate. Then run 'python -m pytest -q' from release-gate to verify, then stage release-gate/ and commit with message 'feat(curveball): dual-format lifecycle-event ingestion with partial-safe results (Track 3 curveball response)'. Do not push.