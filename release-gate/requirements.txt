# Core (pipeline logic runs on stdlib alone; these enable full features)
jsonschema>=4.0          # evidence-bundle validation (graceful fallback if absent)
requests>=2.28           # GitHub PR writeback

# Local dev / test
pytest>=7.0

# ML (Phase 6) and Databricks jobs run on the Databricks runtime, which already
# provides pyspark, mlflow, and scikit-learn/lightgbm. Pinned here for local
# experimentation only.
# lightgbm>=4.0
# scikit-learn>=1.3
# mlflow>=2.9
