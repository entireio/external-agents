command:
  - streamlit
  - run
  - app.py

env:
  - name: RG_CATALOG
    value: release_gate
  # SQL warehouse HTTP path (warehouse IDs are not secrets).
  - name: DATABRICKS_HTTP_PATH
    value: /sql/1.0/warehouses/7f0409edb975227e
