Create release-gate/README.md documenting the Release Gate integration that now lives in this fork of entireio/external-agents. Explain: (1) it is the Track 3 integration built as a NATIVE Entire CLI plugin 'entire release-gate' (an entire-release-gate executable Entire discovers), NOT an app that merely calls entire; (2) install: cd release-gate && pip install -e . then 'entire plugin install' the entire-release-gate executable, then run 'entire release-gate score --base <sha> --pr-number N --pr-repo owner/name --run-tests'; (3) architecture: Entire Graph (blast radius) + Entire Checkpoints (unresolved-risk) -> versioned evidence bundle -> Databricks medallion Delta (bronze/silver/gold, MERGE on pr+sha) -> MLflow model (heuristic fallback) -> PR comment gate; (4) subcommands info/version/collect/score; (5) note the full build history and the four milestone checkpoints also live in the companion mirror. Keep it concise. Then stage release-gate/, BUILDATHON.md, AGENTS.md, CLAUDE.md, and .entire, and commit with message 'feat: add Release Gate as a native entire-release-gate CLI plugin (Track 3 integration in the external-agents fork)'. Do not push.